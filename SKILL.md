---
name: time-aware-context
description: 时间感知上下文校准 skill。当用户消息涉及时间相关概念（日期、时刻、日程、训练计划、提醒等）时，自动获取服务器当前时间并注入上下文，避免因时间误差导致回答错误。适用于训练计划、日程安排、时间计算、时区相关对话。
aliases:
  - time-aware
category: productivity
---

# Time Aware Context

## 触发条件

当且仅当用户消息包含以下**时间相关概念**时触发：

- **绝对时间**：今天、明天、昨天、现在、几点、上午/下午/晚上、具体日期（7/29、2026-07-29）
- **相对时间**：刚刚、刚才、 soon、later、after、before、during
- **日程/计划**：训练、跑步、比赛、会议、提醒、待办、日程、计划
- **时间计算**：还有几天、还剩多少天、X 天后、X 小时、倒计时
- **时区/时间差**：时区、UTC、CST、北京时间、美国时间
- **时间状态**：已过、未到、即将、迟到、提前

## 执行步骤

### 1. 检测时间关键词

扫描用户消息，匹配上述触发词。如果**无任何时间相关概念**，跳过本 skill。

### 2. 获取当前系统时间

执行以下命令获取精确时间：

```bash
date "+%Y-%m-%d %H:%M:%S %Z (%A)"
```

示例输出：
```
2026-07-29 11:52:03 CST (Wednesday)
```

### 3. 注入上下文

将获取到的时间信息**插入到对话上下文中**，格式如下：

```
【当前系统时间校准】
- 服务器时间：2026-07-29 11:52:03 CST (Wednesday)
- 时区：Asia/Shanghai (UTC+8)
- NTP 同步状态：active
```

**关键规则**：
- 时间信息必须放在回答的**最前面**，用醒目的标记包裹
- 如果用户消息包含具体日期（如"7/30 训练"），需明确核对：如果系统时间是 7/29，则 7/30 是"明天"
- 如果用户消息中的时间概念与系统时间不符（如用户说"今天已经跑了"但系统显示还没到训练时间），需主动指出

### 4. 基于校准时间回答

在回答中：
- 使用【当前系统时间校准】中的时间作为基准
- 所有相对时间计算（"还有 X 天"、"X 小时后"）都基于此基准
- 如果涉及未来日期，明确说明是"明天"、"后天"还是具体日期
- 如果涉及过去日期，明确说明是"昨天"、"X 天前"

## 输出格式

```
【当前系统时间校准】
- 服务器时间：2026-07-29 11:52:03 CST (Wednesday)
- 时区：Asia/Shanghai (UTC+8)
- NTP 同步状态：active

[正常回答内容...]
```

## 示例

**用户输入**：我今天 7/30 早上吃了西瓜，训练计划是什么？

**处理流程**：
1. 检测到"今天"、"7/30"、"训练计划" → 触发
2. 执行 `date` 获取当前时间
3. 发现系统时间是 7/29，用户说的"今天 7/30"实际上是"明天"
4. 注入时间校准上下文
5. 回答中纠正：明天（7/30）的训练计划是...

**输出**：
```
【当前系统时间校准】
- 服务器时间：2026-07-29 11:52:03 CST (Wednesday)
- 时区：Asia/Shanghai (UTC+8)
- NTP 同步状态：active

大王，你提到的"今天 7/30"实际上是**明天**（系统当前时间是 7/29）。

根据训练计划，7/30（周四）07:00 的安排是：
- 内容：跑步 5km
- 配速：7:00–7:15，步频 170
- 饮食：跑前碳水；跑后蛋白质+碳水
```

## 注意事项

1. **不要过度触发**：只有明确涉及时间概念时才触发，普通聊天（如"你好"、"谢谢"）不触发
2. **时间校准优先**：如果用户说的时间与系统时间冲突，**以系统时间为准**，并温和地指出差异
3. **时区敏感**：如果用户提到时区转换（如"美国时间"），需额外计算时差
4. **缓存时间**：单次对话中，如果多次触发，应复用第一次获取的时间，避免重复调用

## 故障处理

如果 `date` 命令执行失败：
- 输出警告：`【时间校准失败】无法获取系统时间，以下回答可能基于过期时间`
- 继续回答，但避免精确的时间计算
- 建议用户检查服务器 NTP 服务
